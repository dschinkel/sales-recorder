# TDD Log - Task F.1.3

OUTSIDE-IN FLOW:
UI Component (ReportView) -> State Management (App.tsx)

PLAN:

1. Report Button (Humble View)
   - Goal: Add a "Generate Report" button at the bottom of the list.
   - Changes: App.tsx
   - Verification: Manual check on simulator.

2. Formatted Report Display
   RED:
   - Test name: displays formatted report with all answers
   - Location: __tests__/App.test.tsx
   - Intent: Verify that clicking "Generate Report" shows a view containing each question followed by its corresponding answer (only for answered questions).
   
   GREEN:
   - Minimal implementation:
     - Add a showReport state (boolean).
     - If showReport is true, render a scrollable View (or Modal) that maps over answered questions.
     - Style the report with bold questions and italicized/indented answers.
   - Files expected to change: App.tsx
   
   COMMIT:
   - Proposed message: feat: F.1.3: displays formatted report of answers

3. Refactor
   - Candidate refactor: Extract the Report logic into a dedicated component (src/components/Report.tsx).
   - COMMIT:
   - Proposed message: refactor: F.1.3: extracts Report component

4. Cleanup & Verification
   - Run all tests.
   - Fix linting.
   - Update version label to Version: F.1.3.
   - COMMIT:
   - Proposed message: feat: F.1.3: cleanup and version update
